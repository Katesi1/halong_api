node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables (không commit thông tin thật)
.env
.env.*.local

# Build output
dist
coverage

# Prisma
/generated/prisma
prisma/*.db

# IDE / Editor
.DS_Store
.idea
.vscode
*.swp
*.swo

# Logs
server.log

# OS
Thumbs.db

# Secrets — Firebase service account, GCP keys, etc.
secrets/
*firebase-adminsdk*.json
*service-account*.json
*.pem
*.key

# Deploy / VPS secrets — never commit
.deploy-secrets.json
.pgpassword.txt
.git.broken-bak/

# VPS-local binaries (44MB+ installers, not part of source)
.installers/

# Claude Code local state (lock files, etc.)
.claude/scheduled_tasks.lock
